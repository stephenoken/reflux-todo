let React = require('react');

let Todo = React.createClass({
  render: function () {
      return (
        <div>
          <h1>Todo List</h1>
        </div>
      );
  }
});

module.exports = Todo;
